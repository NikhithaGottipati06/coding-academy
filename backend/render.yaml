services:
  - type: web
    name: coding-academy-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GOOGLE_API_KEY
        value: ""  # Set this in Render dashboard
